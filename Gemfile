source 'https://rubygems.org'

# adds Atom feeds to blog
gem 'jekyll-feed'

# enables local previews of blog
# and ensures that we use the same versions of ruby gems as github pages server
gem 'github-pages', group: :jekyll_plugins
